# ytdlmp3

YT-DLP configuration utility.

# Details

This program allows configuration files to be specified for bulk `yt-dlp` downloads.

# Usage

1. Update the [example configs](configs/).
2. Run `./main.sh`.
